version: '3.9'

services:
  app:
    build:
      context: .
    container_name: laboratorio_glemos
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - .:/reddit-clone
    stdin_open: true
    tty: true
